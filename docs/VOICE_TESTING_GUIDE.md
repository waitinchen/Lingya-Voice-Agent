# 🧪 語音功能測試指南

本指南提供完整的語音功能測試方法，包括自動化測試、手動測試和性能測試。

---

## 📋 目錄

1. [測試環境準備](#測試環境準備)
2. [自動化測試](#自動化測試)
3. [手動功能測試](#手動功能測試)
4. [性能測試](#性能測試)
5. [壓力測試](#壓力測試)
6. [測試場景](#測試場景)

---

## 🔧 測試環境準備

### 1. 本地測試環境

```bash
# 1. 安裝依賴
npm install

# 2. 配置環境變數
cp .env.example .env
# 編輯 .env 填入真實的 API Keys

# 3. 啟動服務器
npm start
```

### 2. 測試工具準備

#### 必需工具
- **curl**: 命令行 HTTP 客戶端
- **瀏覽器**: Chrome/Firefox（支持語音功能）
- **音頻播放器**: VLC/系統播放器

#### 可選工具
- **Postman**: API 測試工具
- **Audio Recorder**: 錄製測試音頻
- **FFmpeg**: 音頻格式轉換

### 3. 測試音頻準備

創建測試音頻文件：

```bash
# 使用系統麥克風錄製（macOS）
sox -d test-audio.wav

# 或使用 FFmpeg 生成測試音頻
ffmpeg -f lavfi -i "sine=frequency=1000:duration=5" test-tone.wav
```

準備多種測試場景的音頻：
- `test-zh.wav` - 中文語音
- `test-en.wav` - 英文語音
- `test-noise.wav` - 帶噪音的語音
- `test-long.wav` - 長音頻（30秒+）
- `test-short.wav` - 短音頻（1秒）

---

## 🤖 自動化測試

### 1. 運行內建測試

#### 情緒標籤測試
```bash
npm test
# 或
node test/run-emotion-tests.js
```

**預期輸出**:
```
✅ 情緒標籤解析測試通過
✅ 語氣參數轉換測試通過
✅ 預設語氣組合測試通過
```

#### 快速測試
```bash
npm run test:quick
```

#### 健康檢查測試
```bash
npm run test:health
```

#### 語音會話測試
```bash
npm run test:voice-session
```

#### TTS 串流測試
```bash
npm run test:tts-stream
```

### 2. 單元測試腳本

#### 測試 OpenAI STT
```bash
node test-openai.js
```

**預期結果**:
- ✅ API Key 有效
- ✅ 能訪問 Whisper API
- ✅ 配額充足

#### 測試 Cartesia TTS
```bash
node test-cartesia.js
```

**預期結果**:
- ✅ API Key 有效
- ✅ Voice ID 正確
- ✅ 生成音頻文件
- ✅ 音頻可播放

#### 測試情緒標籤
```bash
node test-emotion-tags.js
```

**預期結果**:
- ✅ 所有情緒標籤轉換正確
- ✅ 參數範圍合理（pitch: -1 to 1）
- ✅ 預設組合有效

---

## 🖐️ 手動功能測試

### 1. 語音識別（STT）測試

#### 基礎功能測試

```bash
# 測試 1: 中文語音識別
curl -X POST http://localhost:3000/api/transcribe \
  -F "audio=@test-zh.wav" \
  -F "language=zh"

# 預期輸出
{
  "success": true,
  "text": "你好，我是花小軟"
}
```

```bash
# 測試 2: 英文語音識別
curl -X POST http://localhost:3000/api/transcribe \
  -F "audio=@test-en.wav" \
  -F "language=en"

# 預期輸出
{
  "success": true,
  "text": "Hello, I am Hua Xiao Ruan"
}
```

#### 錯誤處理測試

```bash
# 測試 3: 無效音頻格式
curl -X POST http://localhost:3000/api/transcribe \
  -F "audio=@test.txt" \
  -F "language=zh"

# 預期: 返回錯誤信息
```

```bash
# 測試 4: 空音頻文件
curl -X POST http://localhost:3000/api/transcribe \
  -F "audio=@empty.wav" \
  -F "language=zh"

# 預期: 返回錯誤或空文本
```

### 2. 語音合成（TTS）測試

#### 基礎合成測試

```bash
# 測試 1: 簡單文本合成
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{"text":"你好，我是花小軟"}' \
  --output test-output.wav

# 驗證: 播放 test-output.wav
```

```bash
# 測試 2: 長文本合成
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{"text":"今天天氣真好，陽光明媚，微風徐徐。我們一起去公園散步吧，那裡有美麗的花朵和綠油油的草地。"}' \
  --output test-long.wav
```

```bash
# 測試 3: 特殊字符處理
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{"text":"老爸～你好啊！😊"}' \
  --output test-special.wav
```

#### 情緒標籤測試

```bash
# 測試 4: Flirty（撒嬌）
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{
    "text": "老爸～你終於回來啦～",
    "voiceParams": {
      "emotionTags": ["flirty"]
    }
  }' \
  --output test-flirty.wav
```

```bash
# 測試 5: Warm（溫暖）
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{
    "text": "別擔心，一切都會好起來的。",
    "voiceParams": {
      "emotionTags": ["warm"]
    }
  }' \
  --output test-warm.wav
```

```bash
# 測試 6: Excited（興奮）
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{
    "text": "太棒了！我們成功了！",
    "voiceParams": {
      "emotionTags": ["excited"]
    }
  }' \
  --output test-excited.wav
```

```bash
# 測試 7: Whisper（耳語）
curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{
    "text": "噓～這是我們的小秘密。",
    "voiceParams": {
      "emotionTags": ["whisper"]
    }
  }' \
  --output test-whisper.wav
```

#### 串流模式測試

```bash
# 測試 8: Buffer 串流模式
curl -X POST http://localhost:3000/api/speak-stream \
  -H "Content-Type: application/json" \
  -d '{"text":"測試串流模式"}' \
  --output test-stream.wav

# 驗證: 文件大小和播放質量
```

### 3. 完整語音對話測試

#### 瀏覽器測試

1. **打開應用**
   ```
   http://localhost:3000
   ```

2. **文字對話測試**
   - 輸入: "你好"
   - 驗證: ✅ 收到回應 ✅ 語音播放

3. **語音對話測試**
   - 點擊 🎤 按鈕
   - 說話: "今天天氣怎麼樣？"
   - 驗證:
     - ✅ 顯示「正在錄音...」
     - ✅ 識別文字正確
     - ✅ AI 回應合理
     - ✅ 語音播放流暢

4. **多輪對話測試**
   - 進行 5 輪語音對話
   - 驗證:
     - ✅ 上下文連貫
     - ✅ 情緒表達適當
     - ✅ 無明顯延遲

#### API 端點測試

```bash
# 測試完整語音對話流程
curl -X POST http://localhost:3000/api/voice-chat \
  -F "audio=@test-zh.wav" \
  -F "language=zh" \
  -F 'history=[]'

# 預期輸出
{
  "success": true,
  "text": "AI 的文字回應",
  "transcribedText": "識別出的用戶語音",
  "audio": "base64-encoded-audio",
  "history": [...]
}
```

---

## ⚡ 性能測試

### 1. 響應時間測試

#### 測量 STT 響應時間

```bash
# 使用 time 命令測量
time curl -X POST http://localhost:3000/api/transcribe \
  -F "audio=@test.wav" \
  -F "language=zh"

# 記錄: _____ 秒
# 目標: < 2 秒
```

#### 測量 TTS 響應時間

```bash
time curl -X POST http://localhost:3000/api/speak \
  -H "Content-Type: application/json" \
  -d '{"text":"測試"}' \
  --output test.wav

# 記錄: _____ 秒
# 目標: < 2 秒
```

#### 測量完整對話響應時間

```bash
time curl -X POST http://localhost:3000/api/voice-chat \
  -F "audio=@test.wav" \
  -F "language=zh" \
  -F 'history=[]'

# 記錄: _____ 秒
# 目標: < 5 秒
```

### 2. 性能統計檢查

```bash
# 獲取性能統計
curl http://localhost:3000/api/stats

# 檢查以下指標
# - stt.avgDuration (目標: < 2000ms)
# - tts.avgDuration (目標: < 2000ms)
# - llm.avgDuration (目標: < 3000ms)
# - 錯誤率 (目標: < 5%)
```

### 3. 音頻質量測試

#### 檢查項目
- [ ] 發音清晰度: ⭐⭐⭐⭐⭐ (1-5)
- [ ] 語調自然度: ⭐⭐⭐⭐⭐
- [ ] 情緒表達: ⭐⭐⭐⭐⭐
- [ ] 無雜音: ✅ / ❌
- [ ] 音量適中: ✅ / ❌

---

## 💪 壓力測試

### 1. 並發請求測試

#### 使用 Apache Bench (ab)

```bash
# 安裝 ab
# macOS: brew install httpd
# Ubuntu: apt-get install apache2-utils

# 並發 10 個請求
ab -n 10 -c 10 -p test-data.json -T "application/json" \
  http://localhost:3000/api/speak

# 分析結果
# - Requests per second (目標: > 5)
# - Failed requests (目標: 0)
# - Time per request (目標: < 2000ms)
```

### 2. 持續負載測試

```bash
# 持續發送請求 1 分鐘
for i in {1..20}; do
  curl -X POST http://localhost:3000/api/speak \
    -H "Content-Type: application/json" \
    -d '{"text":"測試 '$i'"}' \
    --output test-$i.wav &
done

# 等待所有請求完成
wait

# 檢查
# - 所有文件生成成功: ✅ / ❌
# - 無服務器崩潰: ✅ / ❌
# - 響應時間穩定: ✅ / ❌
```

### 3. 內存洩漏測試

```bash
# 記錄初始內存
curl http://localhost:3000/api/stats | grep memory

# 發送 100 個請求
for i in {1..100}; do
  curl -s -X POST http://localhost:3000/api/speak \
    -H "Content-Type: application/json" \
    -d '{"text":"測試"}' \
    --output /dev/null
done

# 再次檢查內存
curl http://localhost:3000/api/stats | grep memory

# 比較: 內存增長應該有限（< 100MB）
```

---

## 🎬 測試場景

### 場景 1: 新用戶首次使用

**步驟**:
1. 清除瀏覽器緩存
2. 訪問應用首頁
3. 允許麥克風權限
4. 進行首次語音對話

**驗證**:
- [ ] 麥克風權限請求正常
- [ ] UI 引導清晰
- [ ] 首次對話成功
- [ ] 語音播放正常

### 場景 2: 多語言切換

**步驟**:
1. 說中文: "你好"
2. 說英文: "Hello"
3. 混合語言: "Hello 你好"

**驗證**:
- [ ] 中文識別準確
- [ ] 英文識別準確
- [ ] 混合語言處理合理

### 場景 3: 情緒變化對話

**對話流程**:
1. 用戶: "我今天很開心！"（興奮）
2. 用戶: "但是有點累了..."（疲憊）
3. 用戶: "你能安慰我嗎？"（尋求溫暖）

**驗證**:
- [ ] AI 能感知情緒變化
- [ ] 語氣隨情緒調整
- [ ] 回應情感適當

### 場景 4: 長時間對話

**步驟**:
1. 進行 10 輪以上對話
2. 觀察性能變化

**驗證**:
- [ ] 響應時間保持穩定
- [ ] 無內存洩漏
- [ ] 上下文記憶正確
- [ ] 語音質量不下降

### 場景 5: 網絡異常處理

**步驟**:
1. 斷開網絡連接
2. 嘗試語音對話
3. 恢復網絡連接
4. 再次嘗試對話

**驗證**:
- [ ] 顯示友好錯誤信息
- [ ] 不會導致應用崩潰
- [ ] 恢復後功能正常

### 場景 6: 特殊輸入處理

**測試輸入**:
- 空白語音
- 純噪音
- 超長文本（>500字）
- 特殊字符: "!@#$%^&*()"
- Emoji: "😊❤️🎉"

**驗證**:
- [ ] 所有情況都有適當處理
- [ ] 無服務器錯誤
- [ ] 錯誤信息明確

---

## 📊 測試報告模板

### 測試執行信息

- **測試日期**: _______________
- **測試人員**: _______________
- **測試環境**: Local / Railway / Production
- **應用版本**: _______________

### 測試結果摘要

| 測試類別 | 通過 | 失敗 | 跳過 | 通過率 |
|---------|-----|-----|-----|--------|
| 自動化測試 | ___ | ___ | ___ | ___% |
| STT 功能 | ___ | ___ | ___ | ___% |
| TTS 功能 | ___ | ___ | ___ | ___% |
| 完整對話 | ___ | ___ | ___ | ___% |
| 性能測試 | ___ | ___ | ___ | ___% |
| 壓力測試 | ___ | ___ | ___ | ___% |
| **總計** | ___ | ___ | ___ | ___% |

### 性能指標

| 指標 | 實際值 | 目標值 | 達標 |
|-----|--------|--------|------|
| STT 響應時間 | ___ ms | < 2000ms | ✅/❌ |
| TTS 響應時間 | ___ ms | < 2000ms | ✅/❌ |
| 完整對話時間 | ___ ms | < 5000ms | ✅/❌ |
| STT 錯誤率 | ___ % | < 5% | ✅/❌ |
| TTS 錯誤率 | ___ % | < 2% | ✅/❌ |
| 內存使用 | ___ MB | < 500MB | ✅/❌ |

### 發現的問題

| 問題ID | 嚴重程度 | 描述 | 狀態 |
|--------|---------|------|------|
| 1 | 高/中/低 | ___ | 待修復/已修復 |
| 2 | 高/中/低 | ___ | 待修復/已修復 |

### 建議改進項

1. _______________
2. _______________
3. _______________

### 測試結論

- [ ] ✅ 所有核心功能正常
- [ ] ✅ 性能指標達標
- [ ] ✅ 無嚴重問題
- [ ] ✅ 建議發布到生產環境

或

- [ ] ❌ 存在阻塞問題，需要修復後重新測試

---

## 🔗 相關資源

- [VOICE_DEPLOYMENT_GUIDE.md](../VOICE_DEPLOYMENT_GUIDE.md) - 部署指南
- [VOICE_DEPLOYMENT_CHECKLIST.md](./VOICE_DEPLOYMENT_CHECKLIST.md) - 部署檢查清單
- [測試腳本目錄](../test/) - 自動化測試腳本

---

**測試指南版本**: v1.0  
**最後更新**: 2025-11-06  
**適用於**: Lingya Voice Agent v0.1.0+
